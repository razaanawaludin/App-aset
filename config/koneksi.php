<?php
// // 1. Definisikan kredensial database
// $host     = "localhost";     // Server database
// $username = "root";          // Username default XAMPP
// $password = "";              // Password default XAMPP (biasanya kosong)
// $db = "db_asset";   // Nama database yang kita buat di Langkah 1

// // 2. Buat koneksi menggunakan mysqli
// $koneksi = new mysqli($host, $username, $password, $db);

// // 3. Cek apakah koneksi berhasil atau gagal
// if ($koneksi->connect_error) {
//     // Jika gagal, tampilkan pesan error
//     die("Koneksi gagal: " . $koneksi->connect_error);
// } else {
    
// }
?>